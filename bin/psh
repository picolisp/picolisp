#!bin/pico lib.l
# 04jun03abu
# (c) Software Lab. Alexander Burger

(load "@lib/misc.l" "@lib/tmp.l" "@lib/rsa.l" "@lib/http.l")

(client "localhost" (format (car (argv)))
   (pack "@psh?" (pw) "&" (in '("tty") (line T)))
   (line) )

(bye)
